import React from 'react';

const Work = () => {
  return (
    <>
      <section id="work" className="h-screen">
        <h2>Work</h2>
        {/* <div>
          <div>
            {skills.map((skill) => (
              <div key={skill.name}>
                <SkillCard
                  name={skill.name}
                  icon={skill.icon}
                  color={skill.color}
                  content={skill.content}
                />
              </div>
            ))}
          </div>
        </div> */}
      </section>
    </>
  );
};

export default Work;
